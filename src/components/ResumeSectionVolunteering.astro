---
const { items } = Astro.props;
---

{items.map((p) => (
  <div class="mb-8">
    <h3 class="text-2xl text-cyan-500 font-bold mb-2">{p.organization}</h3>
    <p>{p.position}</p>
    <p>{p.startDate} - {p.endDate ? p.endDate : 'Now'}</p>
  </div>
))}